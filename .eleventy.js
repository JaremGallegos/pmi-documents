const rss_plugin = require('@11ty/eleventy-plugin-rss');
const syntax_highlight = require('@11ty/eleventy-plugin-syntaxhighlight');
const fs = require('fs');

module.exports = function(config) {
    
}